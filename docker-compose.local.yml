# Local-only port override — applied with:
#   docker compose -f docker-compose.yml -f docker-compose.local.yml up -d
# Maps the db to 5435 on host (in case 5432-5434 are taken by other projects).
services:
  db:
    ports: !override
      - "5435:5432"
